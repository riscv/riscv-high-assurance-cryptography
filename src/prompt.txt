You are conducting a rigorous, adversarial technical review of a draft document describing a proposed security-critical RISC-V ISA extension. This document is contained in the src/ directory of this project.  The root file is ace.adoc. Consider only ace.adoc and the adoc files included by it. Ignore other files, including other files with the .adoc extension.
Ignore the section "Considering an interruptible ace.mgmt" in the file ace-ISA-unpriv.adoc.

Review the files, reconcile cross-references, and assess whether the proposal is suitable to advance as a candidate RISC-V extension. Treat omissions, ambiguity, and underspecified behavior as potential security and interoperability defects.

Focus especially on:

- Technical correctness of the ISA semantics, encodings, state transitions, privilege interactions, exception/trap behavior, reset behavior, concurrency, ordering, and interactions with base ISA and other extensions.
- Security and soundness: privilege-escalation paths, undefined or implementation-defined behavior, information leakage, side channels where relevant, fault handling, malicious software inputs, malformed instruction streams, and unsafe assumptions about hardware or software.
- Algorithmic properties: termination, bounded resource use, progress/liveness, deadlock/livelock risks, recursion or loops, retry behavior, and behavior under faults or interrupts.
- Specification quality: internal consistency, unambiguous normative language, complete definitions, explicit preconditions/postconditions, compatibility and forward-compatibility, and testability.
- Adherence to published standards and RISC-V conventions. Verify claims against applicable published RISC-V specifications and standards; identify the exact specification/version/section where practical. Distinguish confirmed noncompliance from areas needing clarification.
- Implementability: whether compliant hardware and software implementations can be built interoperably without relying on unstated choices.

For each finding, include:

1. An ID using exactly one of these prefixes:
   - `C` for Critical: security compromise, architectural unsoundness, nontermination, or a defect likely to prevent safe interoperability.
   - `M` for Major: material correctness, completeness, compatibility, or implementability issue.
   - `m` for Minor: clarity, editorial, terminology, organization, or lower-risk issue.
2. A concise title.
3. Severity rationale.
4. Precise document location(s), including filename and section/page/line where available.
5. Description of the issue, including the triggering conditions and plausible consequences.
6. Supporting reasoning and, when relevant, the applicable RISC-V or external standard reference.
7. A concrete proposed resolution, preferably with replacement normative wording or pseudocode.

Do not invent requirements or standards. Clearly label uncertainty, assumptions, and questions that cannot be resolved from the supplied documents. Do not downgrade a defect merely because an implementation might choose a safe interpretation; identify where the specification fails to require one.

Structure the response as:

1. Executive assessment: whether the proposal is ready, conditionally ready, or not ready for candidacy.
2. Findings, ordered by severity (`C`, then `M`, then `m`) and by impact within each severity.
3. Cross-document inconsistencies and missing requirements.
4. Standards-compliance matrix: requirement/standard, relevant document location, assessment, and evidence.
5. Suggested prioritized remediation plan.
6. Remaining review questions and assumptions.

Be exacting: this is a security-critical ISA specification, not a general editorial review.
